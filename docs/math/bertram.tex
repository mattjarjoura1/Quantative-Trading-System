\documentclass[11pt]{article}
\usepackage{amsmath, amssymb}
\usepackage[margin=1in]{geometry}

\title{Bertram Optimal Thresholds for Mean-Reverting Trading}
\author{}
\date{}

\begin{document}
\maketitle

% Document the Bertram (2010) framework for optimal entry/exit
% thresholds that maximise expected return per unit time.
% References: Bertram (2010), Zeng \& Lee (2014)

\section{The Trading Cycle}
% Entry at threshold a, exit at threshold b
% Deterministic profit: (b - a) - cost
% Stochastic duration: first-passage times

\section{Dimensionless Transformation}
% Y = (X - mu) / (sigma / sqrt(2*theta))
% Why: scale-invariant results, simplified integrals

\section{Expected First-Passage Time}
% E[tau] = (sqrt(pi)/2) * integral of exp(y^2)(1 + erf(y)) dy
% Relationship to erfi

\section{Objective Function}
% G(d) = (2d - c') / (2 * E[tau(-d, d)])
% Maximise G w.r.t. d via numerical optimisation

\section{Zeng-Lee Extension: Continuous Long/Short}
% Eliminate dead time by flipping position at exit
% Exit of long = entry of short and vice versa

\section{Transformation Back to Real Coordinates}
% entry = mu - d* . s, exit = mu + d* . s
% where s = sigma / sqrt(2*theta)

\section{Implementation Notes}
% Numerical integration via scipy.integrate.quad
% Optimisation via scipy.optimize.minimize\_scalar
% Bounds: d > c'/2, d < 4

\end{document}
